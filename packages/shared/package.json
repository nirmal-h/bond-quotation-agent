{
  "name": "@bond-quotation/shared",
  "version": "1.0.0",
  "description": "Shared schemas and utilities for bond quotation agent",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "typescript": "~5.4.0"
  }
} 